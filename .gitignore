############################################################
# 1. Build artefacts generated by CMake / Ninja / Make
############################################################
/build*/                 # out-of-source builds: build/, build-debug/, etc.
CMakeFiles/
/CMakeCache.txt
/*.cmake
/Makefile
/install_manifest.txt
/*.ninja
/*.o
/*.obj
/*.so
/*.dll
/*.dylib
/*.a
/*.lib
*.vcxproj*
compile_commands.json    # IDE will regenerate; keep if you rely on it

############################################################
# 2. Test & data junk
############################################################
*.gz                     # large raw ITCH samples you don’t want in git
data/                    # if you drop temporary data sets here
*.log
tests/tmp*

############################################################
# 3. IDE / editor metadata
############################################################
# — VS Code —
.vscode/
*.code-workspace

# — CLion / JetBrains —
.idea/
cmake-build-*/

# — Xcode —
*.xcworkspace
*.xcuserstate
*.xcuserdatad/

# — Visual Studio —
.vs/
*.opensdf
*.sdf
*.suo

# — General —
.DS_Store               # macOS Finder fluff
Thumbs.db               # Windows Explorer fluff
*~                      # backup files from many editors
*.swp                   # Vim swap files
.env                    # secrets (if you store API keys)

############################################################
# 4. External dependencies you *fetch* or *build*, not source-control
############################################################
/extern/*/build*/       # if you add Catch2 or googletest as a submodule
/_deps/                 # FetchContent puts archives here in newer CMake

